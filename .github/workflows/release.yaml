name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'The tag to release without the leading v (e.g. 1.2.3)'
        required: true
      prerelease:
        description: 'Mark this as a prerelease?'
        required: true
        default: true
        type: boolean

permissions:
  contents: write
  actions: read

env:
  VERSION: ${{ github.event.inputs.tag }}
  RELEASE_TAG: v${{ github.event.inputs.tag }}

jobs:
  create-release:
    name: Create Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ env.RELEASE_TAG }}

      - name: Find latest successful Flatpak run on main
        id: flatpak-run
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const workflow_id = "build-flatpak.yaml";
            const branch = "main";
            const response = await github.rest.actions.listWorkflowRuns({
              owner,
              repo,
              workflow_id,
              branch,
              status: "success",
              per_page: 1,
            });

            const run = response.data.workflow_runs[0];
            if (!run) {
              core.setFailed("No successful Build Flatpak workflow run found on main.");
              return;
            }

            core.setOutput("run_id", String(run.id));

      - name: Find latest successful macOS run on main
        id: macos-run
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const workflow_id = "build-macos.yaml";
            const branch = "main";
            const response = await github.rest.actions.listWorkflowRuns({
              owner,
              repo,
              workflow_id,
              branch,
              status: "success",
              per_page: 1,
            });

            const run = response.data.workflow_runs[0];
            if (!run) {
              core.setFailed("No successful Build macOS workflow run found on main.");
              return;
            }

            core.setOutput("run_id", String(run.id));

      - name: Download Flatpak artifact from main build
        uses: actions/download-artifact@v4
        with:
          github-token: ${{ github.token }}
          run-id: ${{ steps.flatpak-run.outputs.run_id }}
          pattern: nextapp-flatpak-*-${{ env.VERSION }}
          path: artifacts/flatpak
          merge-multiple: true

      - name: Download macOS artifacts from main build
        uses: actions/download-artifact@v4
        with:
          github-token: ${{ github.token }}
          run-id: ${{ steps.macos-run.outputs.run_id }}
          pattern: nextapp-macos-*-dmg-${{ env.VERSION }}
          path: artifacts/macos
          merge-multiple: true

      - name: Verify release artifacts exist
        run: |
          set -euo pipefail
          find artifacts -maxdepth 2 -type f | sort
          find artifacts -type f -name "*.flatpak" | grep -q .
          find artifacts -type f -name "*.dmg" | grep -q .

      - name: Show release files
        run: find artifacts -maxdepth 2 -type f | sort

      - name: Create or update GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: Release ${{ env.RELEASE_TAG }}
          draft: false
          prerelease: ${{ github.event.inputs.prerelease }}
          body_path: ${{ github.workspace }}/CHANGELOG.txt
          files: |
            artifacts/**/*.flatpak
            artifacts/**/*.dmg
